buildscript {

    repositories {
        jcenter()
        maven {
            url 'https://oss.sonatype.org/content/repositories/snapshots/'
        }
        mavenLocal();

        /*
        flatDir {
            dirs "${projectDir}/../microdb-gradleplugin/build/libs"
        }
        */
    }


    dependencies {
        classpath 'com.devsmart:microdb-gradleplugin:0.2.0-SNAPSHOT'
    }
}

apply plugin: 'java'
apply plugin: 'com.devsmart.microdb'

dependencies {
    compile 'com.devsmart:microdb-runtime:0.2.0-SNAPSHOT'
}