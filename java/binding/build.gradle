allprojects {

  group = 'com.dev-smart'
  version = '0.1.0-SNAPSHOT'
  description = 'A embedded NoSQL Database'

  repositories {
    jcenter()
  }
}

task wrapper(type: Wrapper) {
  gradleVersion = '2.5'
}