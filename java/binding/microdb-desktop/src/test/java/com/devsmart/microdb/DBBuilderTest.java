package com.devsmart.microdb;


import com.devsmart.IOUtils;

import org.junit.Test;
import static org.junit.Assert.*;

import java.io.File;

public class DBBuilderTest {

    /*
    @Test
    public void testLoad() throws Exception {
        File dbpath = new File("testdb");
        if(dbpath.exists()) {
            IOUtils.deleteTree(dbpath);
        }
        dbpath.mkdirs();

        MicroDB db = DBBuilder.builder(dbpath)
                .schemaVersion(0)
                .build();

        assertNotNull(db);

        db.close();

    }
    */
}
